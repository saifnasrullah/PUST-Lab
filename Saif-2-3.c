#include<stdio.h>
int main()
{
    double length,m,km;
    length=10;
    m=length/100;
    km=m/1000;
    printf("the length is %lf\n",length);
    printf("the length in meter is %lf\n",m);
    printf("the length in kilometer is %lf\n",km);
    return 0;

}
